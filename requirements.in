opencv-python>=4.9.0
numpy>=1.26.0
onnxruntime>=1.27.0
Pillow>=10.3.0
plotly>=7.0.0
PyYAML>=6.0.1
PySide6>=6.7.0
PyInstaller>=6.14.0
hypothesis>=6.0.0
# Sapera LT camera binding: pythonnet loads the machine's own SapClassBasic.dll on the .NET
# Framework (netfx) runtime. The vendor DLL itself is never bundled or redistributed.
pythonnet>=3.1.0
