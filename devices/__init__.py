"""Optional acquisition hardware (line-scan CCD camera and LSI-8181 meter wheel).

Everything in this package is hardware-neutral at import time: vendor SDKs and DLLs are
optional capabilities, so VisionFlow keeps starting when they are missing.
"""
